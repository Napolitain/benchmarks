syntax = "proto3";

package benchmark;

// A simple message for benchmarking serialization
message BenchmarkData {
    int64 id = 1;
    string name = 2;
    double value = 3;
    repeated int32 numbers = 4;
    bytes data = 5;
}

[project]
name = "rectangle"
version = "0.1.0"
description = "Calculate rectangle area from YAML file"
requires-python = ">=3.8"
dependencies = [
    "pyyaml>=5.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
